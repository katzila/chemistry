import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#689275',
    },
    secondary: {
      main: '#5fbd7e',
    },
    background: {
      default: '#DCDCDC',
    },
  },
});
